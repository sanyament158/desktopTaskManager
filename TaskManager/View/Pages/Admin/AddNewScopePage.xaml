<Page x:Class="TaskManager.View.Pages.Admin.AddNewScopePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TaskManager.View.Pages.Admin"
      mc:Ignorable="d"
      Title="AddNewScopePage"
      Background="White">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Values/TextBlockStyles.xaml"/>
                <ResourceDictionary Source="/Values/TextBoxStyles.xaml"/>
                <ResourceDictionary Source="/Values/BorderStyles.xaml"/>
                <ResourceDictionary Source="/Values/ButtonStyles.xaml"/>
                <ResourceDictionary Source="/Values/Strings.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Style="{StaticResource HeaderStackPanel}">
            <TextBlock Text="Добавить новую область"
                       Grid.Row="0"
                       Style="{StaticResource HeaderTextBlock}"
                       HorizontalAlignment="Center"
                       Margin="0 30"/>
        </Border>

        <!-- FORM -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1"
                    Background="{StaticResource grey}"
                    CornerRadius="10"
                    Padding="25"
                    VerticalAlignment="Center">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Имя"
                               Style="{StaticResource FieldTextBlock}"
                               Margin="0 0 0 8"/>

                    <Border Grid.Row="2"
                            Style="{StaticResource BaseStackPanel}">
                        <TextBox Text="{Binding EnteredName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource BaseTextBox}"
                                 FontSize="20"
                                 Padding="10"/>
                    </Border>

                </Grid>
            </Border>
        </Grid>

        <!-- BUTTONS -->
        <Grid Grid.Row="2" Margin="0 20">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">

                <Border Style="{StaticResource ButtonStackPanel}"
                        Margin="10"
                        Padding="10">
                    <Button Content="{StaticResource add}"
                            Style="{StaticResource BaseButton}"
                            Command="{Binding AcceptCommand}"/>
                </Border>

                <Border Style="{StaticResource ButtonStackPanel}"
                        Margin="10"
                        >
                    <Button Content="{StaticResource back}"
                            Style="{StaticResource BaseButton}"
                            Padding="30 10"
                            Command="{Binding GoBackCommand}"/>
                </Border>

            </StackPanel>
        </Grid>

    </Grid>
</Page>