<Page x:Class="TaskManager.View.Pages.Admin.ViewResponsibilities"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TaskManager.View.Pages.Admin"
      mc:Ignorable="d"
      Background="White"
      Title="ViewResponsibilities">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Values/ButtonStyles.xaml"/>
                <ResourceDictionary Source="/Values/BorderStyles.xaml"/>
                <ResourceDictionary Source="/Values/Strings.xaml"/>
                <ResourceDictionary Source="/Values/TextBlockStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Style="{StaticResource HeaderStackPanel}"
                >
            <TextBlock Text="Оценка загруженности сотрудников"
                       Style="{StaticResource HeaderTextBlock}"
                       HorizontalAlignment="Center" Margin="0 30"/>
        </Border>

        <!-- RATIO -->
        <TextBlock Grid.Row="1"
                   Text="{Binding Ratio}"
                   Style="{StaticResource InfoTextBlock}"
                   HorizontalAlignment="Center"
                   Margin="0 10"/>

        <!-- CONTENT -->
        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- EMPLOYEES -->
            <Border Grid.Column="0"
                    Background="{StaticResource grey}"
                    CornerRadius="10"
                    Margin="10"
                    Padding="10">
                <ListView
                              ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedUser}"
                              Background="Transparent"
                              DisplayMemberPath="Lname"
                              Margin="5"
                              BorderThickness="0"
                              FontSize="16"/>


            </Border>

            <!-- TASKS -->
            <Border Grid.Column="1"
                    Background="{StaticResource grey}"
                    CornerRadius="10"
                    Margin="10"
                    Padding="10">

                <ListView ItemsSource="{Binding Tasks}"
          Background="Transparent"
          BorderThickness="0"
          FontSize="16">

                    <ListView.Resources>

                        <Style TargetType="{x:Type GridViewColumnHeader}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Padding" Value="6"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>

                    </ListView.Resources>

                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Заголовок"
                            DisplayMemberBinding="{Binding Title}"
                            Width="220"/>
                            <GridViewColumn Header="Зона"
                            DisplayMemberBinding="{Binding Scope.Name}"
                            Width="220"/>
                        </GridView>
                    </ListView.View>

                </ListView>
            </Border>

        </Grid>

    </Grid>
</Page>