<Page x:Class="TaskManager.View.Pages.Users.TakedTasksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TaskManager.View.Pages.Users"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TakedTasksPage"
      Background="White">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Values/Strings.xaml"/>
                <ResourceDictionary Source="/Values/TextBlockStyles.xaml"/>
                <ResourceDictionary Source="/Values/ButtonStyles.xaml"/>
                <ResourceDictionary Source="/Values/BorderStyles.xaml"/>
                <ResourceDictionary Source="/Values/ComboBoxStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="0.8*"/>
        </Grid.RowDefinitions>
        <Border Style="{StaticResource HeaderStackPanel}" Margin="20">
            <TextBlock x:Name="headerTextBlock" Text="Мои задачи" Style="{StaticResource HeaderTextBlock}"/>
        </Border>

        <ListView x:Name="listViewTasks" Grid.Row="1" ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}"
              BorderThickness="0" Margin="30">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding GoToShowDetailsCommand}" CommandParameter="{Binding SelectedTask}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Владелец" DisplayMemberBinding="{Binding Owner.Lname}"/>
                    <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding Status.Name}"/>
                    <GridViewColumn Header="Заголовок" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Начало" DisplayMemberBinding="{Binding Since, StringFormat=\{0:d\}}"/>
                    <GridViewColumn Header="Дедлайн" DisplayMemberBinding="{Binding Deadline, StringFormat=\{0:d\}}"/>
                    <GridViewColumn Header="Зона ответственности" DisplayMemberBinding="{Binding Scope.Name}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Border Style="{StaticResource ButtonStackPanel}" Margin="250 20 250 20" Grid.Row="2" >
            <Button x:Name="buttonBack" 
                    Style="{StaticResource BigButton}"
                    Content="{StaticResource back}" 
                    Command="{Binding GoBackCommand}"/>
        </Border>
    </Grid>
</Page>
